<%= react_component("Barrel", { satellite: @satellite, barrel: @barrel, barrelID: @barrel.id, lastFlavorSensorResult: @barrel.last_flavor_sensor_result, status: @barrel.status, barrelErrors: @barrel.errors }) %>
